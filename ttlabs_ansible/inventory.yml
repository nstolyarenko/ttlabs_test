all:
  vars:
    ansible_user: terraform
    ansible_ssh_private_key_file: ~/.ssh/id_rsa_test
    ip_cidr_range: "10.22.0.0/24"
    ssh_port: 2222
    vpn_network_cidr: "10.0.0.0/24"
    vpn_listen_port: 51820
  children:
    vpn_group:
      hosts:
        vpn:
          ansible_host: 34.147.181.200
          ansible_port: "{{ ssh_port }}"
          host_index: 0
    db_group:
      hosts:
        db:
          ansible_host: 34.105.243.32
          ansible_port: "{{ ssh_port }}"
          host_index: 1
    mon_group:
      hosts:
        mon:
          ansible_host: 34.105.236.223
          ansible_port: "{{ ssh_port }}"
          host_index: 2

